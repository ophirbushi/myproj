import { Container, Box, Typography } from '@mui/material';

function App() {
  return (
    <Container maxWidth="lg">
      <Box my={2}>
        <Typography variant="h4">Acquire Game UI</Typography>
        {/* Your components go here */}
      </Box>
    </Container>
  );
}

export default App;
